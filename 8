/******************************************************************************
Faça um algoritmo para obter três números inteiros diferentes e exibir os números em ordem crescente
(do menor para o maior).
Casos de teste:
a) Para os números 9, 7 e 5, serão apresentados os números 5, 7 e 9 (nessa ordem);
b) Para os números 10, 20 e 30, serão apresentados os números 10, 20 e 30 (nessa ordem);
c) Para os números 2, 4 e 3, serão apresentados os números 2, 3 e 4 (nessa ordem);
d) Para os números 30, 25 e 35, serão apresentados os números 25, 30 e 35 (nessa ordem);
e) Para os números 22, 23 e 21, serão apresentados os números 21, 22 e 23 (nessa ordem);
f) Para os números 125, 55 e 100, serão apresentados os números 55, 100 e 125 (nessa ordem). 
*******************************************************************************/
#include <stdio.h>

int main()
{
    int a = 0, b = 0, c = 0;
    

    printf("Digite um valor: ");
    scanf("%d", &a);

    printf("Digite um valor: ");
    scanf("%d", &b);
    
    printf("Digite um valor: ");
    scanf("%d", &c);


    if (a <= b && b <= c)
        printf("%d %d %d\n", a, b, c);
    if (a <= c && c <= b)
        printf("%d %d %d\n", a, c, b);
    if (b <= a && a <= c)
        printf("%d %d %d\n", b, a, c);
    if (b <= c && c <= a)
        printf("%d %d %d\n", b, c, a);
    if (c <= a && a <= b)
        printf("%d %d %d\n", c, a, b);
    if (c <= b && b <= a)
        printf("%d %d %d\n", c, b, a);

    return 0;
}
 
